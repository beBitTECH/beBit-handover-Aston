import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  trailingSlash: true,
  basePath: "/beBit-handover-Aston",
  assetPrefix: "/beBit-handover-Aston/",
};

export default nextConfig;
